FROM node

WORKDIR /app

COPY package.json .

RUN npm install

COPY . .

ARG DEFAULT_PORT=80

ENV PORT $DEFAULT_PORT
ENV MONGODB_USERNAME user
ENV MONGODB_PASSWORD secret

EXPOSE $PORT

CMD ["npm", "start"]
